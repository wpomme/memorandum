- map
```javascript
# 値を得るときはget() を使う
const map = new Map();

map.get("key")
// -> key に対応するvalue が返ってくる
```

## JSDoc の書き方
```javascript
/**
 * 
 * @param {Map<string, String>} userMap - ユーザーとユーザーに紐づくMap
 */
```
